type TransferSingle @entity {
  id: ID!
  # address
  operator: String!
  from: String!
  to: String!
  param_id: BigInt!
  value: BigInt!
}

type TransferBatch @entity {
  id: ID!
  # address
  operator: String!
  from: String!
  to: String!
  ids: String!
  values: String!
}
